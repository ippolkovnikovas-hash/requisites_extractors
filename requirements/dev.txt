-r base.txt

# Тесты
pytest==9.1.1
pytest-cov==7.1.0
# reportlab нужен паре тестов, которые генерируют PDF без текстового слоя
# (проверка ветки PDF-скана). В рантайме проекта не используется.
reportlab==5.0.1

# Линтеры и типы
ruff==0.16.6
black==26.5.1
mypy==2.3.1
