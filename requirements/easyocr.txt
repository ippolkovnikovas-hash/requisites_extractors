# Опциональный OCR-бэкенд EasyOCR.
#
# Вынесен из base.txt намеренно: easyocr тянет за собой torch (~500 МБ), а по
# умолчанию проект использует Tesseract. Ставьте, только если действительно
# собираетесь переключить OCR_BACKEND=easyocr.
#
#   pip install -r requirements/easyocr.txt
#
# Версия проверена локально (импортируется, тянет torch/numpy/opencv без
# конфликтов) — CI её не ставит и не тестирует (см. requirements/dev.txt).
easyocr==1.7.2
