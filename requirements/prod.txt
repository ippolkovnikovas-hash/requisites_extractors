-r base.txt

# gunicorn — Unix-only (использует fork()), поэтому версия не проверена ни
# локально на Windows, ни в CI (workflow ставит только dev.txt). Взята
# последняя стабильная версия с PyPI на момент пиннинга (04.09.2026).
gunicorn==26.2.0
